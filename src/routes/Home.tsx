import Hero from "../components/Hero";

export default function Home() {
  return (
    <section>
      <h1>Home</h1>
      <p>Fuentes bloqueantes + hero pesado (baseline).</p>
      <Hero />
    </section>
  )
}
